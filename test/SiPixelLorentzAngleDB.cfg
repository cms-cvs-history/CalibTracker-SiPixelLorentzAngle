process Test = {
    
	source = EmptyIOVSource { string timetype = "runnumber" uint64 firstRun = 1 uint64 lastRun = 1 uint64 interval = 1 }
//     source = EmptyIOVSource {
// 	untracked int32 maxEvents = 1

// 		untracked uint32 firstRun = 1 
// 		untracked uint32 numberEventsInRun = 1
//     }
	untracked PSet maxEvents = {untracked int32 input = 1}
	//        service = MessageLogger {
// 	untracked vstring destinations = { "write_langle_geom_ideal_blob.log" }
// 	untracked PSet write_langle_geom_ideal_blob.log = { untracked string threshold = "INFO" }
//     }
   # Initialize magnetic field
   include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
    # Tracker SimGeometryXML
   include "Geometry/TrackerSimData/data/trackerSimGeometryXML.cfi"
 
   # Tracker Geometry Builder
   include "Geometry/TrackerGeometryBuilder/data/trackerGeometry.cfi"
 
   # Tracker Numbering Builder
   include "Geometry/TrackerNumberingBuilder/data/trackerNumberingGeometry.cfi"

//     service = Timing {}
    
	include "CondCore/DBCommon/data/CondDBCommon.cfi"



	service = PoolDBOutputService {
		string timetype = "runnumber"
# For SQLITE
#	
string connect = "sqlite_file:LorentzAngle.db" 
#   
# For Development DataBase
#    
#        string connect = "oracle://cms_orcoff_int2r/CMS_COND_PIXEL"

		VPSet toPut={ { string record = "SiPixelLorentzAngleRcd"   
                                string tag ="trivial_LorentzAngle"} } 
                PSet DBParameters = { 
	          #untracked string authenticationPath = "/afs/cern.ch/cms/DB/conddb"  
	          untracked string authenticationPath = ""
	          untracked int32 messageLevel = 2
	          }   
	}    
        
// include "CalibTracker/SiPixelLorentzAngle/data/SiPixelLorentzAngleDB.cfi"
	module sipixelLorentzAngle = SiPixelLorentzAngleDB{
		double magneticField = 4.0
	}

       path p = { sipixelLorentzAngle }
        
//        module print = AsciiOutputModule {}
//        endpath ep = { print }
}
