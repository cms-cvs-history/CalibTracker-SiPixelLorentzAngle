process Test = {
    
    source = EmptySource {
// 	untracked int32 maxEvents = 1
	untracked uint32 firstRun = 1   
	untracked uint32 numberEventsInRun = 1
    }
    untracked PSet maxEvents = {untracked int32 input = 1}
//     service = MessageLogger {
// 	untracked vstring destinations = { "read_langle_geom_ideal.log" }
// 	untracked PSet read_langle_geom_ideal_blob.log = { untracked string threshold = "INFO" }
//     }
    
    service = Timing {}
    
    
    es_source = PoolDBESSource { 
		VPSet toGet = {{ string record = "SiPixelLorentzAngleRcd" string tag = "trivial_LorentzAngle" }}
		
		string connect = "sqlite_file:LorentzAngle.db"   
	        #string connect = "oracle://cms_orcoff_int2r/CMS_COND_PIXEL" 
		string timetype = "runnumber" 
		PSet DBParameters = {
			untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
			untracked int32 messageLevel=2
		}
    }
    
module prod =  SiPixelLorentzAngleReader { }
                  

       path p = { prod }

       module print = AsciiOutputModule {}
       endpath ep = { print }
}
